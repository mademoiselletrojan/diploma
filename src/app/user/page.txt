'use client';

import { useState, useEffect } from 'react';
import Header from '../../components/Header/Header';
import UpHeader from '../../components/Topbar/Topbar';
import Link from 'next/link';
// import Library from '../../components/Library';

export default function UserPage() {
  const [activeTab, setActiveTab] = useState('library');

  const mockTexts = [
    {
      id: 1,
      title: 'The Little Prince',
      preview: 'Once when I was six years old I saw a magnificent picture...',
    },
    {
      id: 2,
      title: 'Alice in Wonderland',
      preview: 'Alice was beginning to get very tired of sitting by her sister...',
    },
  ];

  return (
    <div>
      <UpHeader />
      <Header />

      <div className="p-4">
        <div className="flex gap-4 mb-4">
          <button
            onClick={() => setActiveTab('library')}
            className={`px-4 py-2 rounded ${activeTab === 'library' ? 'bg-purple-600 text-white' : 'bg-gray-200'}`}
          >
            Library
          </button>
          <button
            onClick={() => setActiveTab('dictionary')}
            className={`px-4 py-2 rounded ${activeTab === 'dictionary' ? 'bg-purple-600 text-white' : 'bg-gray-200'}`}
          >
            Dictionary
          </button>
        </div>

        {/* {activeTab === 'library' && (
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
            {mockTexts.map(text => (
              <div key={text.id} className="bg-white rounded shadow p-4 border">
                <h2 className="text-lg font-bold mb-2">{text.title}</h2>
                <p className="text-gray-600 mb-4">{text.preview}</p>
                <Link href={`/user/library/${text.id}`}>
                  <span className="text-purple-600 font-semibold hover:underline cursor-pointer">
                    Читать →
                  </span>
                </Link>
              </div>
            ))}
          </div>
        )} */}
        {/* {activeTab === 'library' && <Library userId={id} />} */}

        {activeTab === 'dictionary' && (
          <p>Your personal dictionary (soon).</p>
        )}
      </div>
    </div>
  );
}
